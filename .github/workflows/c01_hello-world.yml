name: Hello-World

# on: [push]
on: workflow_dispatch

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello WORLD !!!!!
        run: echo Hello, World